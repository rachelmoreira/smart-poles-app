import { StatusBar } from "expo-status-bar";
import React from "react";
import { SafeAreaView, Text } from "react-native";

export default function Sound() {
    return <SafeAreaView>
        <StatusBar/>
        <Text>Sons e ruídos</Text>
    </SafeAreaView>
}